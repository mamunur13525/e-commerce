import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["i.ibb.co", "ik.imagekit.io", "localhost", "lh3.googleusercontent.com", "plus.unsplash.com"],

  },

};

export default nextConfig;
